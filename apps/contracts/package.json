{
  "name": "@open-creator-rails/contracts",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "forge build",
    "test": "forge test",
    "clean": "forge clean",
    "lint": "solhint 'src/**/*.sol'"
  },
  "devDependencies": {
    "solhint": "^6.1.0"
  }
}